{
  "name": "vue-cli-template-webpack-42",
  "version": "1.0.0",
  "description": "A Webpack setup with hot-reload, lint-on-save, scss & css extraction.",
  "scripts": {
    "docs": "cd docs && gitbook serve",
    "docs:deploy": "bash ./deploy-docs.sh"
  },
  "devDependencies": {
    "vue-cli": "^2.8.1"
  }
}
